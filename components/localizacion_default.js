import * as React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export default function local_default() {
    const [origin, setOrigin] = React.useState ({
        latitute: 10.488314,
        longitude: -66.855794,
    });
}